# Ignore peer dependency conflicts between @sveltejs/vite-plugin-svelte@4.x
# (declares peer vite@^5) and vite@6 – they work together fine.
# This is equivalent to npm install --legacy-peer-deps.
legacy-peer-deps=true
